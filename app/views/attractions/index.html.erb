<h1>Attractions</h1>

<ul>
  <% @attractions.each do |attraction| %>
    <% if @user.admin == false %>
      <li><%= link_to "Go on #{attraction.name}", attraction_path(attraction) %></li><br>
    <% else %>
      <li><%= link_to "Show #{attraction.name}", attraction_path(attraction) %></li><br>
    <% end %>
  <% end %>
</ul>

<% if @user.admin == true %>
  <%= link_to "New Attraction", new_attraction_path %>
<% end %>

